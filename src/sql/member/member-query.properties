listCount=SELECT COUNT(*) FROM MEMBER
selectList=SELECT RNUM, UNO, USER_ID, USER_NAME, PHONE, ADDRESS, PROFITS, POINT, PENALTY, ENROLL_DATE, STATUS FROM (SELECT ROWNUM RNUM, UNO, USER_ID, USER_NAME, PHONE, ADDRESS, PROFITS, POINT, PENALTY, ENROLL_DATE, STATUS FROM (SELECT UNO, USER_ID, USER_NAME, PHONE, ADDRESS, PROFITS, POINT, PENALTY, ENROLL_DATE, STATUS FROM MEMBER ORDER BY UNO DESC)) WHERE RNUM BETWEEN ? AND ?